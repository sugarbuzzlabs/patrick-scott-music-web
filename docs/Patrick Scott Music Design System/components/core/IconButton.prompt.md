Square bordered icon-only button for social links and compact actions; pass an inline Lucide SVG as children and always set `label`.

```jsx
<IconButton label="Instagram" href="https://instagram.com/..."><svg .../></IconButton>
```
