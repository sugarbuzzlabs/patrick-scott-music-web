Section opener: amber mono eyebrow + heavy expanded uppercase title, optional right action.

```jsx
<SectionHeader eyebrow="Upcoming shows" title="On stage" action={<Button variant="ghost">All dates</Button>} />
```
