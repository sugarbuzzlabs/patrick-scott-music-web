Centered modal on blurred ink scrim; display-type title, square close button. Tip/QR overlays, confirmations.

```jsx
<Dialog open={open} onClose={() => setOpen(false)} title="Tip the band">…</Dialog>
```
